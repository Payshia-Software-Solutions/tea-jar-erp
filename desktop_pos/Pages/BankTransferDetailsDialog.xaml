<Window x:Class="DesktopPOS.Pages.BankTransferDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bank Transfer Details" Height="360" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Grid Background="Transparent">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="400" Height="320" Background="{DynamicResource BgCardElevated}" CornerRadius="16" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource BorderPrimary}">
            <Border.Effect>
                <DropShadowEffect Color="{DynamicResource ShadowColor}" Opacity="0.15" BlurRadius="30" ShadowDepth="4"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="BANK TRANSFER DETAILS" Foreground="{DynamicResource TextPrimary}" FontSize="18" FontWeight="Black"/>
                    <Button Grid.Column="1" Content="✕" Width="26" Height="26"
                            Background="{DynamicResource BgInput}" Foreground="{DynamicResource TextSecondary}" BorderThickness="0"
                            FontSize="10" Click="Cancel_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Inputs -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="TRANSACTION REFERENCE / ID" Foreground="{DynamicResource TextMuted}" FontSize="9" FontWeight="Bold" Margin="0,0,0,6"/>
                    <Border Background="{DynamicResource BgInput}" CornerRadius="8" Height="44" Margin="0,0,0,16" BorderBrush="{DynamicResource BorderPrimary}" BorderThickness="1">
                        <TextBox x:Name="RefNoBox" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" FontSize="15" Padding="12,0" VerticalContentAlignment="Center" CaretBrush="{DynamicResource TextPrimary}"/>
                    </Border>

                    <TextBlock Text="RECEIVING BANK NAME" Foreground="{DynamicResource TextMuted}" FontSize="9" FontWeight="Bold" Margin="0,0,0,6"/>
                    <Button x:Name="SelectBankButton" Click="SelectBank_Click" Height="44" BorderThickness="1" BorderBrush="{DynamicResource BorderPrimary}" Background="{DynamicResource BgInput}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Padding="0" Margin="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimary}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="BankNameText" Text="Click to select bank..." Foreground="{DynamicResource TextSecondary}" FontSize="14" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="▾" Foreground="{DynamicResource TextSecondary}" FontSize="14" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        </Grid>
                    </Button>

                    <TextBlock x:Name="ErrorText" Foreground="{DynamicResource AccentDanger}" FontSize="11" FontWeight="Bold" Margin="0,10,0,0" Visibility="Collapsed"/>
                </StackPanel>

                <!-- Action Button -->
                <Button Grid.Row="2" Content="Confirm Transfer details" Height="42" Click="Confirm_Click"
                        Background="{DynamicResource AccentPrimary}" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="Bold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
